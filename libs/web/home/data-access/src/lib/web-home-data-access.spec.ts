import { webHomeDataAccess } from './web-home-data-access';

describe('webHomeDataAccess', () => {
  it('should work', () => {
    expect(webHomeDataAccess()).toEqual('web-home-data-access');
  });
});
