import { PlaylistLocalRepository } from './playlist-local.repository';

describe('PlaylistLocalRepository', () => {
  it('should create an instance', () => {
    expect(new PlaylistLocalRepository()).toBeTruthy();
  });
});
