import { PlaylistHttpRepository } from './playlist-http.repository';

describe('PlaylistHttpRepository', () => {
  it('should create an instance', () => {
    expect(new PlaylistHttpRepository()).toBeTruthy();
  });
});
