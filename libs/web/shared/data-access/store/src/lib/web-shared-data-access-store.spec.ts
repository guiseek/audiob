import { webSharedDataAccessStore } from './web-shared-data-access-store';

describe('webSharedDataAccessStore', () => {
  it('should work', () => {
    expect(webSharedDataAccessStore()).toEqual('web-shared-data-access-store');
  });
});
