import { webSharedDataAccessModels } from './web-shared-data-access-models';

describe('webSharedDataAccessModels', () => {
  it('should work', () => {
    expect(webSharedDataAccessModels()).toEqual(
      'web-shared-data-access-models'
    );
  });
});
